# Thesz canonical-v2 — profile normalized v4

Versioned profile candidate applying the definitive integer-lattice foot fit from
addenda 6–7 of the 2026-08-26 profile-normalization record. It does not overwrite
normalized v1, v2, or v3, the approved source parts, or frozen Pass-A v3.

Rebuild with `npm run art:normalize-profile-v2`. The tool refuses to run unless
Pass-A v3 remains SHA-256
`ce78aea34da48af54721c6babf74c46c71b29f00810ae26390d9c92cffc3dceb`.

## Changed from normalized v3

- Near/left profile chain: hip `(378,467)`, knee `(371,568)`, ankle
  `(369,666)`, unchanged sole `(385,720)` on front boot A.
- Far/right profile chain: hip `(398,467)`, knee `(391,568)`, ankle
  `(393,666)`, unchanged sole `(441,702)` on rear boot B.
- The ankle gap is 24px. Both thigh spans remain exactly 10250px² and both shin
  spans exactly 9608px².
- Back3q is unchanged from its approved v3 fit.

The very small 0.06px and 0.20px extension margins are a declared structural
constraint, not something this landmark pass may tune. Changing the hip line,
planted-sole drop, or bone spans requires Derek's separate ruling.

All other settled rules remain unchanged: unreflected turned-view limb reuse,
distinct near/far forearms and hands, right-facing boots, solid trunks, natural
thigh contour, transparent pelvis mask, canonical canvases/anchors, and
`pelvisUnderlay -> far leg -> torso+trunks -> near leg -> arms` depth order.

The proof-only alpha seam is intentionally untouched.
